export const metadata = {
  title: 'Ebook Details — Fable',
  description: 'Read the full details, preview and purchase this ebook on Fable — your premium digital reading platform.',
};

export default function EbookDetailLayout({ children }) {
  return children;
}
